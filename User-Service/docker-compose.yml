version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-java-app
    ports:
      - "8001:8001" # Map container port 8001 to host
    environment:
      - JAVA_OPTS=-Xms256m -Xmx512m
    restart: unless-stopped
